const layer = 'utilities';

/** @type {import('unocss').UserShortcuts} */
export const utilitiesShortcuts = [
  ['flex-center', 'flex items-center justify-center', { layer }],
  ['actions-group', 'flex flex-col md:flex-row flex-wrap items-center justify-end gap-3', { layer }]
];
